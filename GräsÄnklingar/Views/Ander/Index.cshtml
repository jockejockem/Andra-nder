@model IEnumerable<GräsÄnklingar.Models.And>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.andNamn)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.intressen)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.andNamn)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.intressen)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.andid }) |
            @Html.ActionLink("Details", "Details", new { id=item.andid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.andid })
        </td>
    </tr>
}

</table>
